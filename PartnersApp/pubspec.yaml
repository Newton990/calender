name: partners_app
description: Luna Partners App
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.15.0
  firebase_auth: ^4.0.0
  google_sign_in: ^6.1.5
  cloud_firestore: ^4.0.0
  firebase_messaging: ^14.0.0
  encrypt: ^5.0.3
  share_plus: ^8.0.0
  local_auth: ^2.2.0
  cupertino_icons: ^1.0.2
  shared_preferences: ^2.2.0
  google_fonts: ^6.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
